# Generated by "yarp cmake" & Santiago Morante & Juan G. Victores 2012, Juan G. Victores 2014

cmake_minimum_required(VERSION 2.6)

IF (ENABLE_robotDevastation)

set(KEYWORD "robotDevastation")

# Find YARP.  Point the YARP_DIR environment variable at your build.
find_package(YARP REQUIRED)
list(APPEND CMAKE_MODULE_PATH ${YARP_MODULE_PATH})

# Find SDL.  Point the SDL_DIR environment variable at your build.
find_package(SDL REQUIRED)
find_package(SDL_image REQUIRED)

# Search for source code.
file(GLOB folder_source *.cpp *.cc *.c)
file(GLOB folder_header include/*.hpp)
source_group("Source Files" FILES ${folder_source})
source_group("Header Files" FILES ${folder_header})

# Automatically add include directories if needed.
foreach(header_file ${folder_header})
  get_filename_component(p ${header_file} PATH)
  include_directories(${p})
endforeach(header_file ${folder_header})

# Inclue any directories needed
include_directories(${YARP_INCLUDE_DIRS})
include_directories(${SDL_INCLUDE_DIR})
include_directories(${SDL_IMAGE_INCLUDE_DIRS} ${SDL_IMAGE_INCLUDE_DIR} ${SDLIMAGE_INCLUDE_DIR})  # SDLIMAGE_INCLUDE_DIR for old
include_directories(${RD_INCLUDE_DIRS})

# Set up our main executable.
if (folder_source)
  add_executable(${KEYWORD} ${folder_source} ${folder_header})
  target_link_libraries(${KEYWORD} ${RD_LIBRARIES} ${YARP_LIBRARIES} ${SDL_LIBRARY} ${SDL_IMAGE_LIBRARY})
  install(TARGETS ${KEYWORD} DESTINATION bin)
else (folder_source)
  message(FATAL_ERROR "No source code files found. Please add something")
endif (folder_source)

ENDIF (ENABLE_robotDevastation)

